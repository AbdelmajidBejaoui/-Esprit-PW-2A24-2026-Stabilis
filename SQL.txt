-- Create the database
CREATE DATABASE IF NOT EXISTS nutrition_db;
USE nutrition_db;

-- Table for aliments (foods)
CREATE TABLE aliments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(255) NOT NULL,
    description TEXT,
    calories INT DEFAULT 0,
    proteines DECIMAL(5,2) DEFAULT 0.00,
    glucides DECIMAL(5,2) DEFAULT 0.00,
    lipides DECIMAL(5,2) DEFAULT 0.00,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Table for recettes (recipes)
CREATE TABLE recettes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(255) NOT NULL,
    description TEXT,
    instructions TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Table for ingredients (linking recipes and aliments)
CREATE TABLE ingredients (
    id INT AUTO_INCREMENT PRIMARY KEY,
    recette_id INT NOT NULL,
    aliment_id INT NOT NULL,
    quantite DECIMAL(5,2) NOT NULL,
    unite VARCHAR(50),
    FOREIGN KEY (recette_id) REFERENCES recettes(id) ON DELETE CASCADE,
    FOREIGN KEY (aliment_id) REFERENCES aliments(id) ON DELETE CASCADE
);

-- Insert some sample data for aliments
INSERT INTO aliments (nom, description, calories, proteines, glucides, lipides) VALUES
('Poulet', 'Blanc de poulet grillé', 165, 31.00, 0.00, 3.60),
('Riz', 'Riz blanc cuit', 130, 2.70, 28.00, 0.30),
('Brocoli', 'Brocoli vapeur', 55, 3.70, 11.20, 0.60);

-- Insert some sample data for recettes
INSERT INTO recettes (nom, description, instructions) VALUES
('Poulet au riz', 'Un plat simple avec poulet et riz', 'Faire cuire le riz. Griller le poulet. Servir ensemble.');

-- Insert sample ingredients
INSERT INTO ingredients (recette_id, aliment_id, quantite, unite) VALUES
(1, 1, 150, 'g'),
(1, 2, 200, 'g'),
(1, 3, 100, 'g');